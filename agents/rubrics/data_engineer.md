Weigh: numbers computed in code with the computation visible in artifacts (40); data
validation performed and stated (20); outputs saved as artifacts with clear names (20);
charts labeled and readable (10); no mental arithmetic anywhere (10 — one instance caps
at 50).
