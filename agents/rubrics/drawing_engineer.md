Weigh: findings cite drawing regions (page + bbox) and rule ids (35); coverage map meets
the threshold (20); topology statements come from the graph, not from squinting at pixels
(20); tag lists complete against the graph (15); reviewer phrasing — recommend/confirm,
never command operations (10).
