Weigh: tests encode the acceptance criteria and pass in the sandbox (40); the change is
minimal and idiomatic for the codebase (25); lint/type checks clean where configured (10);
change summary accurate — files, rationale, risks (15); no debug leftovers or dead code
(10). A failing test suite is an automatic fail regardless of prose.
