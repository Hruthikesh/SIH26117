{"situation": "Compute MTBF from a CSV of failure events.", "call": {"tool": "python", "args": {"code": "import pandas as pd\ndf = pd.read_csv('artifacts/failures.csv', parse_dates=['date'])\nprint(df.shape)\nprint(df.head())"}}, "why": "Print shape and head first; compute in code, never in your head.", "anti_pattern": "Doing arithmetic on dates or floats mentally instead of in pandas."}
{"situation": "Plot a trend and save it for the report.", "call": {"tool": "python", "args": {"code": "import matplotlib\nmatplotlib.use('Agg')\nimport matplotlib.pyplot as plt\n# ... plot ...\nplt.savefig('artifacts/mtbf_trend.png', dpi=150)\nprint('saved')"}}, "why": "Save outputs as files and cite the artifact; never describe an unsaved plot."}
